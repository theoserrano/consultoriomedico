# Configuração do Banco de Dados MySQL
# Copie este arquivo para .env e configure com suas credenciais

# Host do MySQL (geralmente localhost)
DB_HOST=localhost

# Porta do MySQL (padrão: 3306)
DB_PORT=3306

# Usuário do MySQL
DB_USER=root

# Senha do MySQL (ALTERE AQUI!)
DB_PASSWORD=sua_senha_aqui

# Nome do banco de dados
DB_NAME=consultoriomedico

# Fallback para SQLite (deixe false para usar MySQL)
DB_USE_SQLITE_FALLBACK=false

# ============================================================
# Configurações do Firebase Firestore (NoSQL)
# ============================================================

# Caminho para o arquivo de credenciais do Firebase
# Baixe este arquivo do Firebase Console (Configurações > Contas de Serviço)
FIREBASE_CREDENTIALS_PATH=firebase-credentials.json

# ID do Projeto Firebase (opcional, será lido das credenciais)
FIREBASE_PROJECT_ID=

# Modo de modelagem NoSQL: 'embedded' ou 'referenced'
# embedded: dados completos em cada documento de consulta (recomendado para leitura rápida)
# referenced: usa referências entre coleções (similar a FK do MySQL)
FIREBASE_MODELING_MODE=embedded

# Ativar logs detalhados do Firebase
FIREBASE_DEBUG=false
